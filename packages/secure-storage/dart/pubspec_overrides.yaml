# Workspace-local override for solid_fuse — lets this package build and
# analyze in isolation within the monorepo. Consumer apps get their own
# pubspec_overrides.yaml generated by `fuse link`.
dependency_overrides:
  solid_fuse:
    path: ../../solid-fuse/dart
