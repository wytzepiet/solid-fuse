dart/build
dart/.dart_tool
dart/.flutter-plugins
dart/.flutter-plugins-dependencies
dart/.idea
dart/web
# NOTE: dart/{ios,macos,android,linux,windows} are NOT ignored — solid_fuse is
# now an FFI plugin and those folders carry the cargokit build glue. Excluding
# them ships a plugin with no podspec/gradle/cmake (no native build).
