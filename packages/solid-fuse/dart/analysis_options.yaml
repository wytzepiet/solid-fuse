include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    # Vendored fjs (git subtree mirror of wytzepiet/fjs). Treated as
    # third-party: built by cargokit, re-exported via lib/fjs.dart, not linted.
    - lib/src/fjs/**

linter:
  rules:
    use_key_in_widget_constructors: false
