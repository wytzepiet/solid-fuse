import type { BaseProps } from "../types";

export interface SliverCrossAxisGroupProps extends BaseProps {}

export function SliverCrossAxisGroup(props: SliverCrossAxisGroupProps) {
  return <sliverCrossAxisGroup {...props} />;
}
