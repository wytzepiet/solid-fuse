import type { BaseProps } from "../types";

export interface SliverToBoxAdapterProps extends BaseProps {}

export function SliverToBoxAdapter(props: SliverToBoxAdapterProps) {
  return <sliverToBoxAdapter {...props} />;
}
